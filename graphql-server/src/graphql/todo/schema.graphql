type Todo {
  title: String!
  description: String!
  checked: Boolean!
  user: User!
}

type Mutation {
  createTodo(title: String!, description: String!): Todo!
  updateTodo(title: String!, description: String!, boo
}